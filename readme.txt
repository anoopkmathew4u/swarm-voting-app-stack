docker stack deploy mystack --compose-file docker-stack.yml - to create stack
docker stack ls - to list all stack
docker stack services stack-name - it will list all services which part of that stak
docker stack ps stack-name - to list all container in stack
docker stack rm stack-name - to remove stack
docker stack deploy mystack --compose-file docker-stack.yml - to update stack
